import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        alignItems: "center",
        padding: 10,
        height: "100%",
        width: "100%",
        backgroundColor: "#161719"
    }
});